# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/master/kas/schema-kas.json

header:
  version: 14
  includes:
  - ci/base.yml
  - ci/meta-yocto.yml

local_conf_header:
  # disable cmdline and qcomflash from ci/base.yml
  cmdline: |
  qcomflash: |

machine: genericarm64
